import State from '../../src/Server/State';
import { ServerToClientMessage } from '../../src/Constants';

export default class SocketClient {

    constructor() {
        
    }
    
    public onStateConnected = (state: State) => {
        
    }

    public sendMessage = (msg: ServerToClientMessage) => {
        
    }
}